enum CallPageType {
  audio,
  video,
}

enum CallStatus {
  pending,
  active,
  declined,
  ended,
}

enum ConnectionState {
  newConnection,
  connecting,
  connected,
  disconnected,
  failed,
  closed,
}
